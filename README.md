# SyncSSH

```bash
python -m pip install watchdog && brew install sshpass && curl -o sync_to_ssh_server.py https://raw.githubusercontent.com/GZhonghui/SyncSSH/refs/heads/master/sync_to_ssh_server.py
```
